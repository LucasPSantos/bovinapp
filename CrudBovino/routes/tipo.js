/*module.exports = (app) => {
  const { tipoController } = app.controllers;

  // INDEX
  app.get('/tipo', tipoController.index_tipo);
  app.get('/tipo/index', tipoController.index_tipo);

  // CREATE
  app.post('/tipo/create', tipoController.create_tipo);

  // READ
  app.get('/tipo/list', tipoController.list_tipo);

  // // UPDATE
  // app.get('/bovino/edit/:id', tipoController.update_bovino);
  //
  // // DELETE
  // app.get('/bovino/delete/:id', tipoController.delete_bovino);
};*/